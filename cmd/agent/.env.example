# Database Configuration for DSP Agent
# Copy this file to .env and configure for your database

# Database driver: postgres or mysql
DB_DRIVER=postgres

# Database connection details
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=your_database_name
DB_SSLMODE=disable

# Sync Configuration
SYNC_ENABLED=true
SYNC_INTERVAL=30  # seconds
SYNC_QUERY=SELECT * FROM users LIMIT 10

# Master Server
MASTER_HOST=202.10.42.65
MASTER_PORT=8447
AGENT_NAME=laptop-bintang
